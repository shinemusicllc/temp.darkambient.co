# Decisions

Decision | Reason | Impact | Date
--- | --- | --- | ---
Checkbox chá»n nhiá»u email trong admin pháº£i hiá»‡n theo kiá»ƒu overlay trong row thay vÃ¬ chiáº¿m thÃªm chiá»u ngang layout | User muá»‘n láº¥y láº¡i affordance checkbox nhÆ°ng khÃ´ng cho phÃ©p row/list/header bá»‹ giÃ£n ngang hay vÃ¡Â»Â¡ tÃ»â€° lá»‡ cÅ© | Checkbox Ä‘Æ°á»£c anchor tuyá»‡t Ä‘á»‘i trong padding tráº¡i cá»§a row; header compact `title + count + search + 1 icon` váº«n giá»¯ nguyÃªn | 2026-03-21
Admin header phÃ¡ÂºÂ£i giá»¯ layout compact cÅ© vÃ  chá»‰ thay icon `refresh` báº±ng icon `xÃ³a táº¥t cáº£` | User xÃ¡c nháº­n toolbar vá»¡ layout khi má»Ÿ rá»™ng thÃªm nhiá»u action; trá»ng tÃ¢m thá»±c táº¿ chá»‰ lÃ  thay 1 icon mÃ  khÃ´ng Ä‘á»•i nhá»‹p cÃƒn hiá»‡n cÃ³ | Frontend admin pháº£i quay vá» `title + count + search + 1 icon`, khÃ´ng Ä‘Æ°a thÃªm action strip vÃ  checkbox hiá»ƒn thá»‹ vÃ o header/layout chÃ­nh | 2026-03-21
Admin `Xóa tất cả` xóa toàn bộ email khớp inbox/filter/search hiện tại, còn checkbox multi-select vẫn giới hạn theo page và luôn có xác nhận trước khi xóa toàn bộ | User chốt rõ `Xóa tất cả` không được bó hẹp theo pagination nhưng vẫn muốn luồng chọn nhiều email kiểu mailbox dễ hiểu | Backend cần delete-by-scope riêng, toolbar admin bỏ `refresh`, thêm checkbox/action theo page cho multi-select, và confirm dialog trở thành safety rail bắt buộc cho thao tác xóa toàn bộ | 2026-03-21
Bootstrap repo rules/docs trÆ°á»›c khi thÃªm backend | Repo má»›i chÆ°a cÃ³ AGENTS/docs nhÆ°ng task sáº½ gá»“m nhiá»u bÆ°á»›c vÃ  deploy tháº­t | CÃ³ nÆ¡i lÆ°u context/quy Æ°á»›c trÆ°á»›c khi triá»ƒn khai code | 2026-03-20
Chá»n kiáº¿n trÃºc `FastAPI + SQLite + IMAP sync` thay vÃ¬ mailbox-per-alias | PhÃ¹ há»£p nháº¥t vá»›i yÃªu cáº§u catch-all nháº­n má»i Ä‘á»‹a chá»‰ báº¥t ká»³ mÃ  khÃ´ng cáº§n pre-create alias trÆ°á»›c | Backend chá»‰ cáº§n Ä‘á»c inbox trung tÃ¢m, tá»± phÃ¡t hiá»‡n alias má»›i vÃ  group mail theo recipient | 2026-03-20
Triá»ƒn khai app trÃªn VPS qua `127.0.0.1:8012` sau Caddy chung | VPS Ä‘Ã£ cÃ³ Caddy chiáº¿m `80/443`, nÃªn app má»›i khÃ´ng nÃªn bind trá»±c tiáº¿p public ports | Giáº£m xung Ä‘á»™t runtime, chá»‰ cáº§n thÃªm má»™t block `lush.congmail.top` vÃ o Caddy hiá»‡n cÃ³ | 2026-03-20
Quáº£n lÃ½ admin login cá»§a `Lush Temp Mail` qua helper script | TrÃ¡nh sá»­a tay `deploy/.env` trÃªn VPS vÃ  quÃªn redeploy sau khi Ä‘á»•i credential | CÃ³ lá»‡nh váº­n hÃ nh rÃµ rÃ ng `lushtempmail set-admin`, dá»… ghi vÃ o cheat sheet | 2026-03-20
Cho app temp-mail join Docker network `mailstack_default` Ä‘á»ƒ Ä‘á»c IMAP trá»±c tiáº¿p tá»« mailserver container | ÄÆ°á»ng host/public cho `mail.congmail.top` trÃªn VPS gÃ¢y sync fail dÃ¹ mail Ä‘Ã£ vÃ o inbox trung tÃ¢m | Deploy pháº£i giá»¯ `mailstack_default` vÃ  `IMAP_HOST=mail.congmail.top` Ä‘á»ƒ catch-all mailbox luÃ´n Ä‘Æ°á»£c import á»•n Ä‘á»‹nh | 2026-03-20
Giá»¯ message retention 7 ngÃ y vÃ  tÄƒng nhá»‹p sync/polling cho inbox live | Sáº£n pháº©m lÃ  temp-mail, user muá»‘n inbox dá»n tá»± Ä‘á»™ng vÃ  mail má»›i xuáº¥t hiá»‡n nhanh hÆ¡n khi chá» OTP/link/video verify | Backend sáº½ tá»± suppress mail cÅ© sau 7 ngÃ y, UI poll nhanh hÆ¡n vÃ  VPS runtime dÃ¹ng `MAIL_SYNC_INTERVAL_S=4` | 2026-03-20
Chuyá»ƒn alias máº·c Ä‘á»‹nh vÃ  message retention sang 60 ngÃ y, Ä‘á»“ng thá»i thÃªm tráº¡ng thÃ¡i `Quan trá»ng` | User muá»‘n giá»¯ inbox/alias lÃ¢u hÆ¡n Ä‘á»ƒ duyá»‡t video vÃ  cáº§n ghim mail quan trá»ng ngay trong luá»“ng inbox | Alias auto-create/manual máº·c Ä‘á»‹nh sá»‘ng 60 ngÃ y, message auto-delete sau 60 ngÃ y, backend/frontend cÃ³ state `important`; quyáº¿t Ä‘á»‹nh nÃ y supersede retention 7 ngÃ y trÆ°á»›c Ä‘Ã³ | 2026-03-20
TÃ¡i sá»­ dá»¥ng mailbox trung tÃ¢m `contact@congmail.top` lÃ m SMTP sender cho reply/forward | SMTP auth trÃªn VPS Ä‘Ã£ xÃ¡c minh hoáº¡t Ä‘á»™ng vá»›i cÃ¹ng host/credential Ä‘ang dÃ¹ng cho IMAP (`mail.congmail.top`, `STARTTLS 587`) | KhÃ´ng cáº§n thÃªm service gá»­i riÃªng; composer cÃ³ thá»ƒ gá»­i mail tháº­t ngay vá»›i `SMTP_*` máº·c Ä‘á»‹nh fallback tá»« `IMAP_*` | 2026-03-20
Giá»›i háº¡n multi-select vÃ  `Ctrl + A` theo page inbox hiá»‡n táº¡i | Inbox Ä‘ang cÃ³ pagination client-side; náº¿u chá»n xuyÃªn cÃ¡c page sáº½ táº¡o bulk delete â€œáº©nâ€ khÃ³ tháº¥y vÃ  dá»… xÃ³a nháº§m | Batch select/delete chá»‰ tÃ¡c Ä‘á»™ng lÃªn cÃ¡c row Ä‘ang hiá»ƒn thá»‹ á»Ÿ page hiá»‡n táº¡i, nháº¥t quÃ¡n hÆ¡n vá»›i tráº¡ng thÃ¡i nhÃ¬n tháº¥y trÃªn UI | 2026-03-20
DÃ¹ng helper dá»‹ch qua endpoint Google Translate web báº±ng `httpx` thay vÃ¬ thÃªm dependency má»›i | ÄÃ¡p á»©ng yÃªu cáº§u endpoint dá»‹ch nháº¹, giá»¯ `requirements.txt` khÃ´ng Ä‘á»•i vÃ  váº«n auto-detect Ä‘Æ°á»£c source language | Backend cÃ³ thá»ƒ dá»‹ch subject/body sang `vi` ngay trong `backend/app`; váº«n phá»¥ thuá»™c vÃ o service ngoÃ i | 2026-03-20
TÃ¡ch luá»“ng user lookup thÃ nh `user.html` + `/api/public/*` thay vÃ¬ gÃ i vÃ o admin app | User cáº§n giao diá»‡n tá»‘i giáº£n chá»‰ tra cá»©u alias, cÃ²n admin pháº£i giá»¯ dashboard hiá»‡n táº¡i khÃ´ng Ä‘á»•i contract | Frontend user cÃ³ route riÃªng, backend cÃ³ query summary/detail tá»‘i Æ°u theo exact alias `@congmail.top`, admin routes váº«n nguyÃªn | 2026-03-21
Shared login phÃ¢n role `admin` / `user` vÃ  redirect theo role sau khi Ä‘Äƒng nháº­p | User Ä‘Ã£ chá»‘t mÃ´ hÃ¬nh 2 luá»“ng nhÆ°ng váº«n dÃ¹ng chung form login á»Ÿ `/`, Ä‘á»“ng thá»i inbox user khÃ´ng Ä‘Æ°á»£c tiáº¿p tá»¥c mÃªn danh nghÄ©a public route | `admin` sáº½ á»Ÿ dashboard quáº£n trá»‹, `user` sáº½ vÃ o `user.html`, cÃ²n `/api/public/*` chá»‰ cho session role `user`; deploy env/helper pháº£i quán lÃ½ thÃªm `USER_USERNAME` / `USER_PASSWORD` | 2026-03-21
User route manual lookup pháº£i trigger IMAP sync ngay trÆ°á»›c khi Ä‘á»c inbox | Investigation live cho tháº¥y Gmail -> mailbox -> SQLite khÃ´ng chÃ¢Ậm Ä‘Ã¡ng ká»ƒ, nhÆ°ng luá»“ng `user` trÆ°á»›c Ä‘Ã¢y chá»‰ `GET /api/public/inbox` nÃªn cÃ³ thá»ƒ lá»¡ 1 chu ká»³ sync 4 giÃ¢y | ThÃªm `POST /api/public/sync` cho role `user` vÃ  khoÃ¡ `MailSyncService.sync_once()` Ä‘á»ƒ admin/background/user khÃ´ng giáº«m nhau; khi user báº¥m `Kiá»ƒm tra` sáº½ kÃ©o mail má»›i vá» DB trÆ°á»›c khi render danh sÃ¡ch | 2026-03-21

Prefer `IMAP IDLE` with polling fallback over direct mail-server hook for near-realtime inbox updates | The app already reads a central catch-all mailbox via IMAP; `IDLE` gives near-instant pickup without coupling mail delivery to app availability or modifying the mail stack routing path | Alias discovery stays unchanged (`mail -> contact@congmail.top -> app parse recipient`), while backend sync becomes push-like and admin UI can stop forcing a full IMAP sync on every refresh tick | 2026-03-21
Prefer `SSE` for browser inbox updates and use stale-only sync on the user route | The app only needs one-way server push when the SQLite inbox changes; `SSE` is lighter than WebSocket here, and the user page should not hammer `/api/public/sync` while IMAP IDLE is healthy | Admin and user tabs refresh almost immediately after DB changes, while manual user lookup now forces IMAP only when the backend heartbeat is stale instead of on every click | 2026-03-21
Do not use `messages.received_at` alone as an ingestion-latency metric; it reflects parsed message time rather than guaranteed mailbox/app ingest time | Real Gmail-vs-mailserver inspection showed `received_at` can differ from Dovecot store time by a few seconds in either direction because it comes from message headers, while operational latency analysis needs transport or IMAP internal timestamps | Future latency debugging should prefer mailserver logs, IMAP `INTERNALDATE`, or a dedicated `ingested_at` field before blaming UI/VPS speed | 2026-03-21
Persist both `mailbox_received_at` (IMAP `INTERNALDATE`) and `ingested_at` (DB insert time) on every message | Real timestamp debugging needs a mailbox-side timestamp and an app-side timestamp to separate sender delay from app ingestion delay without SSH log spelunking | Existing APIs can expose trustworthy timing fields for admin/debug use, and future latency investigations can compare `mailbox_received_at -> ingested_at -> UI` directly | 2026-03-21
Render HTML email bodies inside a sandboxed iframe and keep a separate cleaned text fallback plus attachment metadata | Raw text fallbacks from many transactional emails can contain CSS, font declarations, or MIME noise, while injecting HTML directly into the app risks layout/CSS bleed | Admin and user readers now preserve CTA/button layouts more reliably, isolate email CSS from the app shell, and expose attachments without breaking the surrounding UI | 2026-03-23
Email reader surfaces should stay visually flat inside the existing white detail panel, with no nested cards and no visible text-fallback toggle | The user wants the mail content to feel native to the current app shell instead of looking like a stack of generated boxes inside boxes | HTML email content now renders directly on the panel background, attachment rows use simple dividers, and the fallback text affordance is removed from both admin and user readers | 2026-03-23
UI tasks must always use the `uncodixfy` skill and must first align with the product's existing design patterns before introducing new structures | Recent UI feedback shows the main failure mode is visual drift: nested/floating cards and invented patterns that break continuity with the existing app shell | Future UI edits should inspect current files first, reuse the established layout language, and treat new visual structures as exceptions rather than the default | 2026-03-23
Subagents should be used primarily for exploration or independent read-only investigation, not as the default path for implementation | Recent feedback shows delegation felt under-evaluated and too loose when critical implementation work was still better handled directly in the main thread | Future project tasks should explicitly assess delegation first and only spawn when the split is clearly independent, valuable in parallel, and allowed by current tooling policy | 2026-03-23
Email translation must preserve HTML structure when a message has `html_body`, and translation should be skipped when the message is already Vietnamese | Flattening HTML mail into translated plain text destroys CTA/layout fidelity, and offering `Ti?ng Vi?t -> Ti?ng Vi?t` translation creates incorrect UI states | Backend translation now returns `translated_html` plus same-language skip metadata; admin and user readers keep iframe rendering for translated HTML and hide translation affordances for Vietnamese messages | 2026-03-23
Run `temp.darkambient.co` as an isolated Docker Mailserver/Rspamd stack and keep apex Google Workspace unchanged; configure Rspamd with `use_esld=false` | The temp-mail service needs independent catch-all receive/send/reply/forward behavior without changing the production MX records for `darkambient.co`; exact subdomain DKIM lookup otherwise normalizes to the apex | Only the subdomain MX/A/TXT records and VPS stack are owned by this deployment; SPF/DKIM alignment is scoped to `temp.darkambient.co` and apex mail continues through Google Workspace | 2026-07-21
Publish DarkAmbient in a new public `temp.darkambient.co` repository with one clean initial commit and no upstream relationship | The deployed app must be maintained as its own product rather than as a fork or operational continuation of the reference repository | The canonical local checkout and `/opt/darkambient-temp-mail/app` track only `origin/main`; runtime secrets/data stay ignored and deployments use the checked-in health-gated update script | 2026-07-21
